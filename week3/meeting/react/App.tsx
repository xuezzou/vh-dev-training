import * as React from 'react';

// let's define some data
// const data: number[] = [];
// for (let i = 0; i < 100; i++) data[i] = Math.random();

const App = () => {
	// return data.map(el => <div style={{ backgroundColor: 'gray', marginBottom: 5 }}> {el} </div>);

	return <div>hello</div>;
};
export default App;
